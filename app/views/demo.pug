extends layout

block content
  each val, lib in libs
    div.lib(id=lib)
      h1.lib__header= lib
      div.lib__demo
        != val.demo
      div.lib__docs
        != val.docs
      if val.test
        div.lib__test
          != val.test
      script(src=val.js)
