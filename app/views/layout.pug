doctype html
html
  head
    title= helpers.buildTitle(config.name, locals.view, subtitle)
    include _favicon
    if helpers.isProd
      link(rel='stylesheet', href='/app.css')
  body(id=locals.view)
    include _header
    block content
    script(src='/app.js')
