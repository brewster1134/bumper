doctype html
html
  head
    title= helpers.buildTitle(config.app.title, locals.view, subtitle)
    if helpers.isProd
      link(rel='stylesheet', href='/app.css')
      link(rel='stylesheet', href='/user_app.css')
  body(id=locals.view)
    block content
    script(src='/app.js')
    script(src='/user_app.js')
